---
# Homebrew installer and dependencies
- name: Homebrew path
  block:
    - name: Set homebrew path
      set_fact:
        homebrew_path: "{{ '/opt/homebrew' if ansible_machine == 'arm64' else '/usr/local' }}"

